/**
 * Barrel export for the GenericComponentRenderer module.
 * Re-exports the main component and utilities for external consumers.
 */

export { GenericComponentRenderer } from './GenericComponentRenderer';
export type { GenericComponentRendererProps } from './types';
export { MAX_DEPTH, DEFAULT_BOUNDS } from './types';
